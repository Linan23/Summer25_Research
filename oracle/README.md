# Oracle Implementations

This directory contains independent oracle code used for validation. The main
oracle is the Java triangulation implementation in `oracle/java/`.

Oracle checks are for confidence and regression detection. They do not replace
the C++ solver as the maintained implementation under study.

