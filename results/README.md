# Generated Results

`results/` is the default local destination for regenerated sweeps, parity CSVs, plots, profile dumps, shared-convex instances, and temporary experiment outputs.

This directory is ignored by git except for this policy file. Curate only compact, stable summaries into `benchmarks/` when they document a reproducible research claim or the current project state.
